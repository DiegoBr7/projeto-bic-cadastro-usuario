<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Producao</title>
</head>

<body>


    <div class="flex justify-center items-center h-screen bg-indigo-600">
        <div class="w-full max-w-2xl p-6 shadow-lg bg-white rounded-md">
            <h1 class="text-3xl block text-center font-semibold"><i class="fa-solid fa-user"></i>Tabela Producao</h1>
            <hr class="mt-3">
            <div class="mt-3">
                <form action="/producao" method="post">

                    <div class="id">
                        <label class="block text-base mb-2" for="id">ID da Produção</label>
                        <input
                            class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-600"
                            type="number" name="id" id="id" placeholder="Insira o número da produção" required>
                    </div>

                    <div class="email mt-3">
                        <label class="block text-base mb-2" for="email">Email Responsável</label>
                        <input
                            class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-600"
                            type="email" name="email" id="email" placeholder="Insira o email" required>
                    </div>

                    <div class="tempo mt-3">
                        <label class="block text-base mb-2" for="tempo">Tempo de Produção (horas)</label>
                        <input
                            class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-600"
                            type="text" name="tempo" id="tempo" placeholder="Ex: 5h30min" required>
                    </div>

                    <div class="custo mt-3">
                        <label class="block text-base mb-2" for="custo">Custo de Produção (R$)</label>
                        <input
                            class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-600"
                            type="number" step="0.01" name="custo" id="custo" placeholder="Ex: 1500.50" required>
                    </div>

                    <div class="mt-5">
                        <button
                            class="border-2 border-indigo-700 bg-indigo-700 text-white py-1 w-full rounded-md hover:bg-transparent hover:text-indigo-700 font-semibold"
                            type="submit">Salvar Produção</button>
                    </div>
                </form>

            </div>
            <% if (producoes && producoes.length > 0) { %>
                
                <h2 class=" text-xl font-semibold mt-6 ">Producoes cadastradas</h2>
                
            <table class=" w-full border " >
                <thead>
                    <tr class="bg-indigo-200">
                        <th class="p-2 border">ID</th>
                        <th class="p-2 border">EMAIL</th>
                        <th class="p-2 border">TEMPO</th>
                        <th class="p-2 border">CUSTO</th>
                    </tr>
                </thead>
                <tbody>
                    <% producoes.forEach(p => { %>
                    
                        <tr>
                            <td class="p-2 border"><%= p.id %></td>
                            <td class="p-2 border"><%= p.email %></td>
                            <td class="p-2 border"><%= p.tempo %></td>
                            <td class="p-2 border"><%= p.custo %></td>

                        </tr>

                        <% }) %>
                        
                    </tbody>
                </table>
                <% } %>


        </div>
    </div>

</body>

</html>